import React from 'react';
import './Header.css';

function Header() {
  return (
    <div className="head">
      <div className="logo">QuizNow</div>
      <button className="logout-btn">Logout</button>
    </div>
  );
}

export default Header;
